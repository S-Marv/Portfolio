export class Link{
    text:string;
    iconId:string;
    url:string;

    constructor(text:string, iconId:string, url:string){
        this.text = text;
        this.iconId = iconId;
        this.url = url;
    }
}